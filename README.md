Sources for the plugin `plugin.flurry.analytics`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "plugin.flurry.analytics" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
